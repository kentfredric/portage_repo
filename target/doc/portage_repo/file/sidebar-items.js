initSidebarItems({
 "struct": [
  ["Categories", "Decode `${repo}/profiles/categories`"]
  , ["CommentedFileReader", "Decode line based files, skipping lines starting with \"#\""]
  , ["RepoName", "Decode `${repo}/profiles/repo_name`"]
 ]
});
